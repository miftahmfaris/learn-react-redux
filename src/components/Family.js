import React from "react";
import { Person } from "./Person";

export const Family = props => {
    return (
        <section>
            <Person />
            <Person />
            <Person />
        </section>
    );
};
