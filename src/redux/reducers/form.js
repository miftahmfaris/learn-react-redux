const initialState = {
    name: '',
    age: ''
}

const form = (state = initialState, action) => {
    switch (action.type) {
       
        default:
            return state;
    }
}

export default form